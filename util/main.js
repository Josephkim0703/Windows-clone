import { apps } from "./app.js";

/*
function pinApp(apps){

    let data = "";
    let pinned = [];

    for(let i = 0; i < apps.length; i++){
        data += '<button type="button" id="app' + i + '>' + apps[i].logo + '</button>';
        pinned.push(data);
    }
    document.getElementById("pin_taskbar").innerHTML = pinned;
}

pinApp(apps);*/